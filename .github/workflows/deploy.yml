name: Deploy Top Shelf Websites`non:`n  push:`n    branches:`n      - main`njobs:`n  deploy:`n    runs-on: ubuntu-latest`n    steps:`n      - name: Checkout`n        uses: actions/checkout@v4`n      - name: Deploy to Cloudflare Pages`n        uses: cloudflare/pages-action@v1`n        with:`n          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}`n          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}`n          projectName: topshelfwebsites
